# ai-toolkit

> **Confidence**: High
> **Model**: claude-sonnet-4-6
> **Last analyzed**: 2026-04-10 13:29 UTC
> *This file is auto-generated by CodeGraph. Do not edit manually.*

## Summary

ai-toolkit is a self-contained training and inference framework targeting diffusion-based generative models for images and video. It supports a broad set of model architectures including Stable Diffusion (1.x/2.x/XL), FLUX, Wan, HiDream, Chroma, CogView, OmniGen2, and others. Core capabilities include: (1) Parameter-efficient fine-tuning via LoRA and LyCORIS network adapters with configurable rank, alpha, and target-module selection; (2) Adapter subsystems for IP-Adapter (image-prompt conditioning), ControlNet (spatial conditioning), reference adapters, and vision adapters; (3) A dataset pipeline with latent pre-caching and text-embedding caching to accelerate repeated training runs; (4) Support for multiple optimizers (AdamW, Prodigy, CAME, etc.) and precision modes (fp16, bf16, fp8); (5) VAE fine-tuning utilities; (6) Model merging and LoRA extraction tools; (7) A SQLite-backed job queue with a worker process and a UI backend (likely a REST or WebSocket server) for orchestrating and monitoring training jobs through a graphical interface. The repository is structured as a single Python project with no detected cross-repository dependencies; it depends on the standard diffusion-model ecosystem (diffusers, transformers, PyTorch, accelerate, safetensors, etc.) and exposes its functionality through Python scripts and the UI backend. Nothing in the analysis indicates external repositories depend on it.

## Projects

### ai-toolkit

ai-toolkit is a comprehensive training and inference framework for diffusion-based image and video generation models, supporting a wide array of architectures (Stable Diffusion, FLUX, Wan, HiDream, Chroma, CogView, OmniGen2, etc.). It provides tooling for LoRA/LyCORIS network training, adapter systems (IP-Adapter, ControlNet, reference adapters, vision adapters), dataset management with latent/text-embedding caching, various optimizers, VAE training, model merging/extraction, and a UI backend for orchestrating training jobs via a SQLite-backed worker.

